[package]
name = "plugin_interface"
version = "1.1.0"
edition = "2021"
description = "Stable ABI interface shared between MAMESET Cleaner and its ROM system plugins"
license = "MIT"

[dependencies]
abi_stable = "0.11.3"
