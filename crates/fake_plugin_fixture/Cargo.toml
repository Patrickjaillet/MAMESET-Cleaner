[package]
name = "fake_plugin_fixture"
version = "1.1.0"
edition = "2021"
description = "Minimal fake ROM system plugin used only to test dynamic plugin loading"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
abi_stable = "0.11.3"
plugin_interface = { path = "../plugin_interface" }
