export component ResultsView inherits Rectangle {
    in property <string> title: "Résultats";

    VerticalLayout {
        padding: 24px;
        alignment: start;

        Text {
            text: title;
            font-size: 20px;
            font-weight: 700;
        }
    }
}
