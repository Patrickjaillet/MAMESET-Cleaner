export component ScanView inherits Rectangle {
    in property <string> title: "Scan";

    VerticalLayout {
        padding: 24px;
        alignment: start;

        Text {
            text: title;
            font-size: 20px;
            font-weight: 700;
        }
    }
}
